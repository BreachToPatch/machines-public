# Apache 2.4.51 — Hardened configuration for CVE-2021-41773
# ============================================================
# Patch by @MohamedOutougane
# Applied hardening:
#   1. Base image updated from 2.4.49 to 2.4.51 (upstream fix for path traversal)
#   2. Root directory / is now denied by default (defense-in-depth)

ServerRoot "/usr/local/apache2"

Listen 80

# Core modules
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule authz_host_module modules/mod_authz_host.so
LoadModule auth_basic_module modules/mod_auth_basic.so
LoadModule access_compat_module modules/mod_access_compat.so
LoadModule alias_module modules/mod_alias.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule env_module modules/mod_env.so
LoadModule headers_module modules/mod_headers.so
LoadModule unixd_module modules/mod_unixd.so

# CGI module (kept for service parity — no longer dangerous with the denied root)
LoadModule cgi_module modules/mod_cgi.so

ServerAdmin webmaster@localhost
ServerName bee-path

DocumentRoot "/usr/local/apache2/htdocs"

# -----------------------------------------------------------------------
# HARDENED BLOCK — Root directory is denied by default.
# This is defense-in-depth: even if a future CVE bypasses path normalization,
# the traversal cannot reach anything outside of explicitly allowed directories.
# -----------------------------------------------------------------------
<Directory />
    AllowOverride none
    Require all denied
</Directory>

<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

ScriptAlias /cgi-bin/ "/usr/local/apache2/cgi-bin/"

<Directory "/usr/local/apache2/cgi-bin">
    AllowOverride None
    Options ExecCGI
    Require all granted
</Directory>

# Logging
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 combined

# MIME types
TypesConfig conf/mime.types
AddType application/x-compress .Z
AddType application/x-gzip .gz .tgz

# Directory index
DirectoryIndex index.html